
    <!-- ============================ Call To Action ================================== -->
    <section class="theme-bg call-to-act-wrap" style="background:{{ $ps->call_bg }}">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">

                    <div class="call-to-act">
                        <div class="call-to-act-head">
                            <h3>{{ $ps->call_title }}</h3>
                            <span>{{ $ps->call_subtitle }}</span>
                        </div>
                        <a href="{{ $ps->call_button_link }}" class="btn btn-call-to-act">{{ $ps->call_button }}</a>
                    </div>

                </div>
            </div>
        </div>
    </section>
    <!-- ============================ Call To Action End ================================== -->
